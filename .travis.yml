language: node_js

node_js:
  - "0.11"

install:
  - npm install -g typescript
  - npm install -g bower
  - npm install -g grunt-cli
  - npm install -g tsd
  - npm install
  - bower install

script:
  - grunt

notifications:
  email: false
